# Smart copy: joins wrapped prose lines, preserves code and structure
map ctrl+shift+c kitten smart_copy.py

# Raw copy fallback (standard behavior, preserves all newlines)
map ctrl+shift+y copy_to_clipboard

# Strip trailing whitespace from soft-wrapped lines on copy
strip_trailing_spaces smart
